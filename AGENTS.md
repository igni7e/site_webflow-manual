# AGENTS.md - site_webflow-manual

## プロジェクト概要

クライアント向け Webflow 更新マニュアル。Astro Starlight 採用。日本語デフォルト。

## 編集規則

- 全 `.md` ファイルは **frontmatter 必須**：`title` / `description` / `sidebar.order`
- ファイル名は `NN-スラッグ.md`（NN は2桁数字、カテゴリ内連番）
- 本文に `#1` `#超重要` 等のタグ記法は使わない（Obsidian側ルールに準拠）
- 見出しは H1=タイトル、H2=主要セクション、H3=サブセクション
- Webflow 用語は英語表記を維持（Editor, Designer, Publish, CMS, Asset Panel 等）
- マニュアル本文では、読者の判断を助けるためにStarlightのコールアウトを積極的に使う
- 本文用コールアウトは `:::tip[...]` `:::note[...]` `:::caution[...]` を使い、公開前チェック、注意点、判断基準、個人情報の扱い、問い合わせ前の準備などを強調する
- コールアウトは単なる装飾ではなく、初心者が「ここで何に注意するか」「いつ相談するか」を判断できる内容にする

## カテゴリ

- `01-getting-started/` はじめの一歩（招待〜ログイン〜ダッシュボード）
- `02-editor/` Editor モード操作
- `03-cms/` CMS（ブログ・お知らせ）
- `04-designer/` Designer モード（注意付き）
- `05-settings/` SEO・フォーム・ドメイン等
- `06-troubleshooting/` Q&A・トラブル解決

## ターゲット読者

非エンジニアのクライアント担当者。専門用語は初出時に必ず日本語で説明する。

## キャプチャー撮影ルール

- キャプチャーが未撮影のページには、SVG仮画像ではなくStarlightのコールアウトで `キャプチャー指示` を入れる
- コールアウトは `:::note[キャプチャー指示]` 形式を使い、撮影する画面、保存ファイル名、撮影直前の状態、必ず写すもの、写さないものを明記する
- 本文中で画像を差し込む予定位置がある場合も、仮画像ではなく `:::note[キャプチャー差し込み位置]` のコールアウトで示す
- 撮影指示は `docs/human-capture-shot-list.md` と整合させ、画像は原則 `src/assets/captures/manual/` に保存する前提で書く
- Webflowなどの管理画面を撮影する時は、画面遷移後すぐに撮らず、数秒待ってロード完了を確認する
- `Loading...`、スピナー、スケルトンUI、画像未読込、空白カードが残っている状態のキャプチャーは使わない
- 撮影前にページタイトル、主要見出し、左メニュー、対象ボタンや対象フォームが表示されていることを確認する
- 画像やフォーム送信履歴などが遅れて表示される画面では、必要に応じて再読み込み後に再度待機してから撮影する
- 撮影後は、保存した画像を目視確認し、ロード中の状態が写っていないものだけを本文に差し込む

## コミットメッセージ

`feat:` `fix:` `docs:` `chore:` プレフィックス。日本語OK。
